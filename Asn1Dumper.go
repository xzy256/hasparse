package main

/*type Asn1Dumper struct {
	Asn1Builder strings.Builder
}

func (this *Asn1Dumper) ParseAndDumpBytes(content []byte) {
	buffer := bytes.NewBuffer(content)
	parseResult := ParseBuffer(*buffer)
	dumpParseResult(0, parseResult)
}

func (this *Asn1Dumper) DecodeAndDumpBytes(content []byte) {
	buffer := bytes.NewBuffer(content)
	decodeAndDump(buffer)
}

func (this *Asn1Dumper) DecodeAndDumpBuffer(content bytes.Buffer) {
	value := Asn1.decode(content)
dumpType(0, value)
}
*/